export async function helpCommand() {
  console.log(`
Vive UI CLI - Commands:
  add     → Download and add a component to your project
  list    → Show all available components
  help    → Show this help message
  exit    → Quit CLI
  `);
}
